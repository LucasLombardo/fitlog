// This file contains environment variables for the Angular app (production build).
// The Angular CLI replaces environment.ts with this file when building for production.

export const environment = {
  // Base URL for backend API (production)
  apiUrl: 'https://api.fitlogapp.com',
};
